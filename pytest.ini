[pytest]
testpaths = tests
python_files = test_*.py
markers =
    smoke: Smoke tests for health check
    fieldcheck: Input validation tests
    dbcheck: DB-related error tests
    conflict: Uniqueness conflict tests